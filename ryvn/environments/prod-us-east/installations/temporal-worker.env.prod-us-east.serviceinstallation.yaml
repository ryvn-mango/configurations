kind: ServiceInstallation
metadata:
  name: temporal-worker
spec:
  service: temporal-worker
  environment: prod-us-east
  resources:
    cpuCores: 0.5
    memoryMB: 512
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 10
    temporal:
      connection: production
      taskQueue: temporal-worker-queue
      targetQueueSize: 5
