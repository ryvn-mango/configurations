kind: ServiceInstallation
metadata:
  name: frontend-nextjs
spec:
  service: frontend-nextjs
  environment: cohere
  config: |
    resources:
      requests:
        cpu: 0.1
        memory: 512Mi
    service:
      port: 3000
    ingress:
      enabled: true
      className: external-nginx
      annotations:
        cert-manager.io/cluster-issuer: external-issuer
        nginx.ingress.kubernetes.io/proxy-body-size: 5g
        nginx.ingress.kubernetes.io/proxy-buffer-size: 32k
      hosts:
        - host: frontend-nextjs.{{ default "example.local"
            .ryvn.env.state.public_domain.name }}
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - frontend-nextjs.{{ default "example.local"
              .ryvn.env.state.public_domain.name }}
          secretName: frontend-nextjs-ryvn-tls
  env:
  - key: API_URL
    value: http://backend-fastapi:8000
