kind: Service
metadata:
  name: backend-fastapi
spec:
  type: server
  repo: ryvn-mango/fastapi
  build:
    dockerfile: Dockerfile
    branches:
      - name: staging
      - name: main
  promotionPipeline: product-team-pipeline
